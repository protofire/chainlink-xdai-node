# Chainlink on xDAI docker-compose
Just run `docker-compose up`

# Config
Change the node's API user/password by changing the `api` file

Change the keystore's password by modifying the `password` file
